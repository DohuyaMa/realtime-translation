# This file is kept for IDE compatibility only.
# The actual build configuration is in the repository root pyproject.toml.
[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "real-time-translator-dev"
version = "0.1.0"
description = "Dev-only stub — use root pyproject.toml for builds"
