[tool:pytest]
timeout = 30
timeout_method = thread
addopts = -v --tb=short
markers =
    integration: mark test as integration test
    gpu: mark test as requiring GPU
    systemd_update: marks tests related to systemd update implementation
    nix_integration: marks tests for nix integration
    socket_communication: marks tests for socket communication
    runtime_config: marks tests for runtime configuration